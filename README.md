# Local Development

## Prereqs

- [Node](https://nodejs.org/en/download/) >= 8.9.0
- Install [Parcel](https://parceljs.org/) with: `npm install -g parcel-bundler`

Then run any of the following from the project root:

```bash
# Local dev server with HOT reloading and auto-module importing
npm start
```
